#include "dedup.h"
#include <stdio.h>
#include <cstring>

int main() {
        printf("%d", 1000);
        char* pt;
        char a[] ={"Help me mofo"};
        //strcpy(pt, a);
        printf("%u %u", (int)a, (int)pt);
}
